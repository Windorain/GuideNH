package com.hfstudio.guidenh.guide.internal.editor.autocomplete;

public enum AttrType {
    STRING, INT, FLOAT, BOOLEAN, COLOR, ENUM,
    ITEM_ID, BLOCK_ID, ORE_DICT, ENTITY_ID,
    KEY_BIND, PAGE_PATH, FILE_PATH, QUEST_UUID,
    COMMAND, EXPRESSION, DOMAIN, FORMAT_PATTERN,
    VECTOR3, SNBT
}
